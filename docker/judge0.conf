# Judge0 CE Configuration
# Generate strong passwords before production use

POSTGRES_HOST=judge0-db
POSTGRES_DB=judge0
POSTGRES_USER=judge0
POSTGRES_PASSWORD=judge0secret

REDIS_HOST=judge0-redis
REDIS_PASSWORD=

# Security
SECRET_KEY=your-judge0-secret-key-change-me

# Limits
MAX_CPU_TIME_LIMIT=10
MAX_CPU_EXTRA_TIME=5
MAX_WALL_TIME_LIMIT=20
MAX_MEMORY_LIMIT=256000
MAX_STACK_LIMIT=64000
MAX_PROCESSES_AND_OR_THREADS=60
MAX_MAX_FILE_SIZE=1024
MAX_NUMBER_OF_RUNS=20

ENABLE_WAIT_RESULT=true
ENABLE_COMPILER_OPTIONS=false
ENABLE_NETWORK=false
